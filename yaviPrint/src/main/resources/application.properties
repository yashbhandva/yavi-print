# Server Configuration
server.port=8080
server.servlet.context-path=/api

# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/yaviprint_db
spring.datasource.username=root
spring.datasource.password=password

# JPA Configuration (disabled for now)
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration,org.springframework.boot.autoconfigure.orm.jpa.HibernateJpaAutoConfiguration,org.springframework.boot.autoconfigure.security.servlet.SecurityAutoConfiguration

# File Upload
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# JWT Configuration
jwt.secret=yaviPrintSecretKey2024ForJWTTokenGenerationAndValidationSecureAndLong
jwt.expiration=86400000
jwt.refresh-expiration=604800000

# Email Configuration (Gmail SMTP)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=y14379196@gmail.com
spring.mail.password=nlkh tzlh ffnb tjhr
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com

# CORS Configuration
app.cors.allowed-origins=http://localhost:3000,http://localhost:8080,https://localhost:3000
app.cors.allowed-methods=GET,POST,PUT,PATCH,DELETE,OPTIONS
app.cors.allowed-headers=*
app.cors.exposed-headers=Authorization,Content-Disposition,X-Total-Count,X-API-Version
app.cors.allow-credentials=true
app.cors.max-age=3600

# Environment-specific CORS (override in application-dev/prod.properties)
app.frontend.url=https://yaviprint.com
app.admin.url=https://admin.yaviprint.com
# App Configuration
app.email.verification-url=http://localhost:8080/api/auth/verify-email

# Error handling
server.error.include-message=always
server.error.include-binding-errors=always
server.error.include-stacktrace=never
server.error.include-exception=false

# Logging
logging.level.com.web_app.yaviPrint.exception=DEBUG
logging.level.com.web_app.yaviPrint.controller=INFO

# Flyway Configuration
spring.flyway.enabled=false
spring.flyway.locations=classpath:db/migration
spring.flyway.baseline-on-migrate=true
spring.flyway.baseline-version=0
spring.flyway.validate-on-migrate=true
spring.flyway.clean-disabled=true
spring.flyway.out-of-order=false
spring.flyway.ignore-missing-migrations=false
spring.flyway.ignore-future-migrations=false

# Flyway Schema History Table
spring.flyway.table=flyway_schema_history

# Flyway Placeholders (for environment-specific configurations)
spring.flyway.placeholders.app_schema=yaviprint
spring.flyway.placeholders.default_charset=utf8mb4
spring.flyway.placeholders.default_collation=utf8mb4_unicode_ci